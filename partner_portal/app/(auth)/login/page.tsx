'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';
import {useToast} from '@/components/toast';import {Button,Card} from '@/components/ui';
export default function Login(){const r=useRouter();const {toast}=useToast();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [err,setErr]=useState('');const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setErr('');try{const x=await fetch('/api/session',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await x.json();if(!x.ok)throw new Error(d.message);const role=d.user.role;toast({title:'Signed in',message:`Opening ${role.replace('_',' ')} workspace.`,tone:'success'});r.push(role==='admin'?'/admin':role==='warehouse_manager'?'/warehouse':'/vendor')}catch(e){const message=e instanceof Error?e.message:'Login failed';toast({title:'Sign-in failed',message,tone:'error'});setErr(e instanceof Error?e.message:'Login failed')}finally{setBusy(false)}}return <main className="grid min-h-screen place-items-center bg-ink p-6"><div className="grid w-full max-w-5xl overflow-hidden rounded-3xl bg-white shadow-2xl md:grid-cols-2"><div className="hidden bg-mint p-10 md:block"><div className="text-2xl font-black">Near<span className="text-leaf">Now</span></div><div className="mt-20 max-w-sm"><div className="mb-3 inline-flex rounded-full bg-white px-3 py-1 text-xs font-bold text-leaf">OPS CONTROL CENTER</div><h1 className="text-5xl font-black leading-tight">Move orders. Move inventory. Move faster.</h1><p className="mt-5 text-gray-600">One workspace for admins, warehouse teams and vendors.</p></div></div><div className="p-7 md:p-12"><div className="mb-10 md:hidden text-2xl font-black">Near<span className="text-leaf">Now</span></div><h2 className="text-3xl font-black">Welcome back</h2><p className="mt-2 text-sm text-gray-500">Sign in with your NearNow account.</p><form onSubmit={submit} className="mt-8 space-y-5"><label className="block text-sm font-semibold">Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="focus-ring mt-2 w-full rounded-xl border border-line px-4 py-3 outline-none" placeholder="you@nearnow.com"/></label><label className="block text-sm font-semibold">Password<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} className="focus-ring mt-2 w-full rounded-xl border border-line px-4 py-3 outline-none" placeholder="••••••••"/></label>{err&&<div className="rounded-xl bg-red-50 p-3 text-sm font-medium text-red-700">{err}</div>}<Button disabled={busy} className="w-full">{busy?'Signing in…':'Sign in'}</Button><a href="/forgot-password" className="block text-center text-sm font-semibold text-leaf">Forgot password?</a></form><p className="mt-6 text-xs text-gray-400">Role routing is based on the backend's verified <code>role</code> field.</p></div></div></main>}
